# Known issues

- This is a partial §12 stage-1 checkpoint, not the specified complete game. No stage is claimed complete. Stages 2–6 have not started: all three new heroes, talents, subclasses, armor abilities and §9 content remain absent.
- Geometry abstraction is partial. Sewer terrain/walls use 64px frames; logical world units stay at upstream scale. Hero/mob/item frame migration, complete constructor abstraction, new Sewer decor and the full painted art direction remain unfinished.
- Lighting covers region ambient, hero, decorative walls and persistent fire/electric/frost/corrosion blobs. Transient electric/curse sources, burning-character sources and lighting on walls rendered after characters remain unfinished. Low-HP vignette, level-lifetime blood decals and green corrosion particles are implemented.
- Tests 4 and 17 fail the full art gate: `Incomplete art inventory: 104 existing sheets lack pipeline specs`; all three new hero sheets are missing; full character/item style and referenced-frame coverage checks are unfinished. Output: `Validated 36 generated specifications; 5 failures.` The generated-subset diagnostic passes and is not a substitute for full acceptance.
- Test 15 fails: `No enum constant ...HeroClass.NECROMANCER`, likewise ENCHANTER and PSYCHIC; `Runs=30 failures=30`. Logs are in `verification/new-class-smoke.log`. The six upstream heroes separately passed 60 generator/save-load runs; those runs are not new-class or combat acceptance.
- Tests 5–14 and 16 are not run as specified because the new gameplay and full frame-coverage harness are absent. Tests 18–19 are not run: five-region screenshot luminance and 1,000 simulated combat turns with lighting have not been verified. No Android device/emulator gameplay test has run.
- Test 21 is pending remote execution. The workflow retains full art and new-class gates, so a green delivery is not expected at this checkpoint. The passing-state tag `v0.1.0-oneshot` has not been created.
- The latest release v3.3.8 contains six existing heroes, whereas the specification assumes five/eight total. Preserving upstream requires nine total after the additions. Upstream also already uses WARDEN for a Huntress subclass; the Enchanter needs a distinct internal enum key.
- Environment failures: bundled Git reported `git: 'remote-https' is not a git command`; the full `C:\Program Files\Git\cmd\git.exe` works. Sandboxed network calls failed with `An attempt was made to access a socket in a way forbidden by its access permissions`; `.git` writes failed with `Permission denied`. Environment-required escalation succeeded for cloning, setup and OpenGL verification.
- Toolchain setup initially failed on Android repository XML/checksum parsing, then the new sdkmanager wrapper split package names (`Package platforms not found`). Parsing was fixed and the native Android CLI installed platform 36/build-tools 36.0.0 using slash-separated names. It still returned nonzero after installation; actual APK builds succeeded. SDK XML-version/deprecation warnings remain.
- The initial OpenGL probe failed with `InterlevelScene.mode is null`; the probe now initializes the descent mode and title/Sewer rendering passes. The probe writes test slot 99 under the isolated Grimhollow save directory.
