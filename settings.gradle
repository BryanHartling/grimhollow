//core game code modules
include ':SPD-classes'
include ':core'

//platform modules
if (!providers.gradleProperty('desktopOnly').map { it.toBoolean() }.getOrElse(false)) {
    include ':android'
}
// iOS is outside this fork's Windows/Android delivery targets.
include ':desktop'

//service modules
include ':services'
    //updates
    include ':services:updates:debugUpdates'
    include ':services:updates:githubUpdates'
    //news
    include ':services:news:debugNews'
    include ':services:news:shatteredNews'
